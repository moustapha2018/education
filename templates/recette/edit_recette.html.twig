{% extends 'base.html.twig' %}

{% block title %} Recette!{% endblock %}

{% block body %}
    <div class="dashboard-content-one">
        <!-- Breadcubs Area Start Here -->
        <div class="breadcrumbs-area">
            <h3>Recette</h3>
            <ul>
                <li>Acueil</li>
                <li>
                    <a href="{{ path('recette') }}">Les Recettes</a>
                </li>
                <li>Modification Recette</li>
            </ul>
        </div>
        <!-- the formStart Here -->
        <div class="card height-auto">
            <div class="card-body">
                {% for label, messages in app.flashes %}
                    {% for message in messages %}
                        <div class="alert alert-{{ label }} mt-10">
                            {{ message }}
                        </div>
                    {% endfor %}
                {% endfor %}
                <div class="heading-layout1">
                    <div class="item-title">
                        <h4>Modifier une recette </h4>
                    </div>
                    <div class="dropdown">
                        <a class="dropdown-toggle float-right" href="#" role="button" data-toggle="dropdown"
                           aria-expanded="false">...</a>

                        <div class="dropdown-menu dropdown-menu-right">
                            <a class="dropdown-item" href="{{ path('home') }}"><i
                                        class="fas fa-times text-orange-red"></i>Quitter</a>
                            <a class="dropdown-item" href="{{ path('edit_recette', {'id': recette.id}) }}) }}"><i
                                        class="fas fa-redo-alt text-orange-peel"></i>Actualiser</a>
                            <a class="dropdown-item" href="{{ path('recette') }}"><i
                                        class="fas fa-eye text-dark-pastel-green"></i>Voir les recettes</a>
                        </div>
                    </div>
                </div>

                {{ form(form) }}
            </div>
        </div>
        <!-- and the form-->
    </div>


{% endblock %}
