{% extends 'base.html.twig' %}
{% block title %}La recette{% endblock %}
{% block body %}

    <div class="container" >
        <div class="mt-3">
            {% for label, messages in app.flashes %}
                {% for message in messages %}
                    <div class="alert alert-{{ label }} mt-10">
                        {{ message }}
                    </div>
                {% endfor %}
            {% endfor %}
        </div>
        <h6 class="my-3">Details de la recette</h6>

        <div class="row">
            <div class="col-md-12">
                <div class="card bg-light mb-3">
                    <div class="card-header text-center">
                        {{ recette.Rcategorie.name }}/<strong>{{recette.speciality}}</strong><br>
                        <small>{{ recette.createdAt| date('d/m/Y ' ) }}</small>
                    </div>
                    <img class="mx-auto d-block" src="{{ asset('uploads/recettes/'~ recette.image ) }}" alt="image dd"
                         style="height: 60%; width:100%; display:block">
                    <div class="card-body">
                        <h4 class="card-title">
                            {{ recette.name }}
                        </h4>
                        <p class="card-text">
                            {{ recette.description}}
                        </p>
                        <a href="{{ path('home') }}" class="btn btn-secondary">Retour à la page d'accueil</a>
                    </div>
                </div>
            </div>
        </div>
    </div>

{% endblock %}